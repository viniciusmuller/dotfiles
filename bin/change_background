# Uses the helper ./_fzfimg to preview images and set them as background
# This script depends on fzf, nitrogen and ueberzug being installed
file=$(find $WALLPAPERS_DIR | _fzfimg -0 -1 -m)
nitrogen --set-scaled --save $file
